name: "Pipeline 4 - Workers GPU (Cluster Profesor)"

on:
  push:
    branches: [main]
    paths:
      - "Pilar2/P5/worker.py"
      - "Pilar2/P5/gpu-server.py"
      - "Pilar2/P5/brute_force_range.cu"
      - "Pilar2/P5/Dockerfile.worker"
      - "k8s/profesor/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  REGION: us-central1
  REPO: sdypp

jobs:
  gitleaks:
    uses: ./.github/workflows/gitleaks.yml

  unit-tests:
    name: GPU Unit Tests
    needs: gitleaks
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r Pilar2/P5/requirements.txt

      - name: Run GPU unit tests
        run: |
          pytest Pilar2/P5/tests/unitario/test_worker_gpu_publish.py -v

  build-gpu-image:
    name: Build GPU Worker Image
    needs: 
      - gitleaks
      - unit-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_WIF_SERVICE_ACCOUNT }}

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build and push GPU worker image
        run: |
          IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/${{ env.REPO }}/blockchain-worker-gpu"
          docker build -t "${IMAGE}:${{ github.sha }}" -t "${IMAGE}:latest" \
            -f Pilar2/P5/Dockerfile.worker Pilar2/P5/
          docker push "${IMAGE}:${{ github.sha }}"
          docker push "${IMAGE}:latest"

  deploy-profesor:
    name: Deploy to Profesor Cluster
    needs: build-gpu-image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBE_CONFIG_PROFESOR }}" | base64 -d > ~/.kube/config

      - name: Update image tags and deploy
        run: |
          REGISTRY="${{ env.REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/${{ env.REPO }}"
          cd k8s/profesor

          sed -i "s|image: IMAGE_TAG|image: ${REGISTRY}/blockchain-worker-gpu:${{ github.sha }}|" gpu-server-deployment.yaml
          sed -i "s|image: IMAGE_TAG|image: ${REGISTRY}/blockchain-worker-gpu:${{ github.sha }}|" worker-deployment.yaml

          kubectl apply -f .
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: |
          pip install -r Pilar2/P5/requirements.txt

      - name: Wait for GPU deployment
        run: |
          kubectl rollout status deployment/blockchain-gpu-server
          kubectl rollout status deployment/blockchain-worker-gpu

      - name: Run GPU integration tests
        run: |
          pytest Pilar2/P5/tests/integracion/test_worker_gpu_server.py -v
